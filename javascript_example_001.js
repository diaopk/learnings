// example 001 shows that how to use the localStorage 
document.querySelector('from').addEventListener('submit',
  function(e) {
    // get the value from document with element id 'task'
    const task = document.getElementById('task').value;
    let tasks = [1,2,3,4];
});
